import { WebSocketController }    from './WebSocketController'

export const webSocketControllerInstance = new WebSocketController();
// export const webSocketControllerInstance = { } as any;
// webSocketController.registerCallback(this.messageCallback);
// webSocketController.send({type:'imboss', data: ''});
