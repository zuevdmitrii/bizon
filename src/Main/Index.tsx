import * as React from "react";
import { PageTemplate } from "./PageTemplate";

export const Index = (props: any) => {
  
  return (
    <PageTemplate>
      <div>
        Test cmp
      </div>
    </PageTemplate>
  );
};
